import { WeekLabelPipe } from './week-label.pipe';

describe('WeekLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
